version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: silver-walks-api
    restart: always
    ports:
      - "5000:5000"
    env_file:
      - .env

